name: Build Confectionery APK V4 Backup

on:
  push:
    branches: [confectionery-v4-backup-professional]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore stable prototype signing key
        run: base64 -d .ci/confectionery-test-signing.jks.b64 > confectionery_order_app/ci-test-signing.jks

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Build debug APK
        working-directory: confectionery_order_app
        run: gradle --no-daemon :app:assembleDebug --stacktrace

      - name: Copy APK
        run: cp confectionery_order_app/app/build/outputs/apk/debug/app-debug.apk Confectionery-Order-Book-V4.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Confectionery-Order-Book-V4-Backup-APK
          path: Confectionery-Order-Book-V4.apk
          if-no-files-found: error
